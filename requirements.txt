# Runtime deps for the lean waypoint FastAPI agent (server.py).
# Does NOT depend on the vendored strands_pg package.
boto3>=1.35
defusedxml>=0.7
httpx>=0.27
psycopg[binary]>=3.2
fastapi>=0.115
uvicorn[standard]>=0.30
python-dotenv>=1.0
